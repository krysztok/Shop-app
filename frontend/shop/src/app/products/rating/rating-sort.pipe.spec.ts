import { RatingSortPipe } from './rating-sort.pipe';

describe('RatingSortPipe', () => {
  it('create an instance', () => {
    const pipe = new RatingSortPipe();
    expect(pipe).toBeTruthy();
  });
});
