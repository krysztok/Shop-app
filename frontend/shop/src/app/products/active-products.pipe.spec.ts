import { ActiveProductsPipe } from './active-products.pipe';

describe('ActiveProductsPipe', () => {
  it('create an instance', () => {
    const pipe = new ActiveProductsPipe();
    expect(pipe).toBeTruthy();
  });
});
